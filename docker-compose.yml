version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: calendar_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    restart: always
